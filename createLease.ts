import 'dotenv/config'

// Fonction vide, le travail est maintenant dans le contrôleur.
export default async function createNewLease() {
  console.log('Le script de test createLease.ts a été désactivé. Utilisez le ContractsController.')
}
